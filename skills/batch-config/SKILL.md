---
name: batch-config
description: "批量配置管理技能。当用户需要同时修改多台设备的配置时激活，如批量修改屏幕亮度、超时时间等。"
---

# 批量配置管理

通过循环调用单设备配置工具实现批量操作。

## 使用方法

1. 先用 `list_devices` 筛选出目标设备列表
2. 向用户确认要修改的设备数量和范围
3. 逐台调用 `update_config` 执行修改
4. 汇报执行结果：成功数量、失败数量、失败原因
5. **重要**：批量操作前必须让用户确认，避免误操作
